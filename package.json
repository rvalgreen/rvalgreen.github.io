{
  "name": "rvlopes_portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp index.html main.js dist/ && cp -r public/* dist/ && tailwindcss -i ./style.css -o ./dist/style.css --minify",
    "preview": "npx serve dist",
    "deploy": "gh-pages -d dist --cname rvlopes.pt"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "tailwindcss": "^3.4.7"
  }
}
